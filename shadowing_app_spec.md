# 英語シャドーイング学習アプリ 仕様書（v1.0 MVP）

## 1. 概要

毎日短い英語ニュースを取得し、5ステップのシャドーイング練習を行うAndroidアプリ。
Chris Lonsdaleの言語習得メソッド（TEDx「How to Learn Any Language in Six Months」の5原則・7行動）を練習フローに組み込み、継続状況をマス目カレンダー（GitHubコントリビューショングラフ風）で可視化する。

- プラットフォーム：Android（Capacitor）
- 開発体制：ソロ開発、Claude Codeを活用
- 収益モデル：広告（AdMob）中心
- 開発を流用する既存資産：CHAIN ARROWS（Capacitor 8.5.0構成）、障害対応アラームアプリ（Firebase構成の知見）

---

## 2. コンテンツソース

### 2.1 ニュース取得元
- **VOA Learning English** の公式RSSフィードを使用（無料・著作権上の配布制限が緩やか）
- RSSには記事テキストに加え、ネイティブがゆっくり読み上げた**公式MP3音声**が同梱されており、これを「お手本音声」としてそのまま利用する
- 取得タイミング：アプリ起動時に確認 + WorkManagerによる裏更新（1日1回）
- 未読の最新記事1本を「今日のニュース」として提示
- 記事本文はローカルDBにキャッシュ。音声ファイルもダウンロードしオフライン再生に対応

### 2.2 コンテンツの分割
- 記事は2〜3文単位の「段落（セグメント）」に分割
- シャドーイング練習は段落単位で進行（1記事＝複数セグメントの連続練習）

---

## 3. シャドーイング練習フロー（5ステップ）

Chris Lonsdaleの7行動のうち、⑥（Copy the Face／口元ミラー）と⑦（Direct Connect／画像による意味提示）は**MVPスコープ外**とする。以下の5ステップのみ実装する。

| Step | 名称 | 対応する行動 | 内容 |
|---|---|---|---|
| 1 | 意味理解 | ② 意味を先に掴む | セグメントの日本語要約（プレーンテキスト）を先に提示。翻訳全文ではなく要点のみ |
| 2 | 多聴（Brain Soaking） | ① Listen a lot | お手本音声を3回、聞き流し再生（リピート強制、理解を問わない） |
| 3 | シャドーイング録音 | - | お手本再生に重ねて自分の声を録音 |
| 4 | フィードバック | ⑤ Language Parent | 音声認識でテキスト化→原文とのテキスト類似度スコアを算出。減点方式ではなく「伝わる度」として肯定的に提示 |
| 5 | アウトプット | ③ Mixing / Use as a tool | 同じテーマについて自分の一言を自由録音（採点なし、保存のみ） |

補足：④ Glue Words（頻出機能語のハイライト）は、セグメント表示時に接続語・機能語（the, that, would, because 等）を色分けハイライトする形で常時組み込む（独立画面ではなく表示上の工夫として実装）。

### 3.1 発音スコアリングの実装方針
- **フェーズ1（MVP）**：Android標準 `SpeechRecognizer` で音声→テキスト化し、原文とのレーベンシュタイン距離 or 単語一致率でスコア化。厳密な発音評価ではなく「明瞭さ・再現率」の目安とする
- **フェーズ2（将来検討）**：Speechace / Azure Pronunciation Assessment等の専用APIによる音素レベル評価。従量課金のため広告収益が安定してから検討。スコアリング処理はインターフェースで分離し、後から差し替え可能な設計にしておく

---

## 4. 継続記録（マス目カレンダー）

- GitHubコントリビューショングラフ風のマス目UI。1日1マス、月〜年単位で表示
- 色の濃淡は**達成度**に応じて変化：
  - 未実施：グレー
  - 一部のセグメントのみ実施：薄緑
  - 全セグメント＋5ステップ完了：濃緑
  - 高スコア達成：特別色（ボーナス演出）
- マスをタップすると詳細表示（その日読んだニュース、スコア、録音再生）
- ストリーク（連続日数）をホーム画面ヘッダーに表示

---

## 5. マネタイズ

- バナー広告：ホーム画面下部に常時表示
- インタースティシャル広告：1日のシャドーイングセッション完了後、1日1回まで表示
- （将来検討）リワード広告：フェーズ2の精密発音スコア機能を1回分アンロック

---

## 6. 画面構成

| 画面 | 内容 |
|---|---|
| ホーム | 今日のニュース、「シャドーイングを始める」ボタン、簡易マス目カレンダー、ストリーク表示 |
| シャドーイング練習 | 5ステップフローを1セグメントずつ進行。完了後、次セグメントへ |
| 記録カレンダー | マス目カレンダー全体表示、日別詳細モーダル |
| 履歴 | 過去に読んだニュース一覧、再挑戦可能 |
| 設定 | リマインダー通知時刻、データリセット、広告表示設定等 |

---

## 7. 技術構成

- **フレームワーク**：Capacitor（CHAIN ARROWSと同構成を流用）
- **音声録音**：capacitor-voice-recorder等のプラグイン
- **音声認識**：Android `SpeechRecognizer` 呼び出し用プラグイン（既存プラグインがなければ簡易ネイティブブリッジを自作）
- **データ保存**：ローカル完結（Capacitor Preferences または SQLiteプラグイン）。アカウント登録不要、バックエンド（Firebase等）は今回は不要と判断し運用コストを削減
- **RSS取得**：起動時フェッチ＋WorkManagerによる裏更新
- **広告**：AdMob（バナー＋インタースティシャル）

---

## 8. MVPスコープ外（将来検討）

| 項目 | 理由 |
|---|---|
| ⑥ Copy the Face（口元ミラー、インカメラ活用） | カメラ処理が絡み実装コストが高いため |
| ⑦ Direct Connect（キーワードの画像/アイコン提示） | 画像素材の調達・管理コストがかかるため。当面は②の日本語要約で意味理解を代替 |
| 発音スコアの精密化（Speechace等の専用API） | 従量課金のため、広告収益が安定してから検討 |
| リワード広告による機能アンロック | フェーズ2の精密スコア機能実装後に検討 |

---

## 9. 未決定事項（今後の相談ポイント）

- 1日の学習量の目安（1記事のみか、複数記事対応か）
- リマインダー通知の有無・タイミング
- オフライン時の挙動（音声未ダウンロード時のフォールバック）
- VOA RSSの記事が短すぎる/長すぎる場合の扱い
